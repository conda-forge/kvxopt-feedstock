diff --git a/src/C/dense.c b/src/C/dense.c
--- a/src/C/dense.c
+++ b/src/C/dense.c
@@ -158,7 +158,12 @@ matrix * Matrix_New(int nrows, int ncols, int id)
     return NULL;
   }
 
-  a->id = id; a->nrows = nrows; a->ncols = ncols;
+  a->id = id; a->nrows = nrows; a->ncols = ncols;
+  /* Python 3.14 buffer protocol compatibility */
+  a->ob_exports = 0;
+  a->shape[0] = nrows;
+  a->shape[1] = ncols;
+  a->strides[0] = ncols * E_SIZE[id];
+  a->strides[1] = E_SIZE[id];
   if ((a->buffer = calloc(nrows*ncols,E_SIZE[id])))
     return a;
   else if (nrows*ncols == 0) 
